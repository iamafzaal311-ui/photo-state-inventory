name: Flutter Web Deploy to Netlify

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Java (Required for stable building)
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Setup Flutter
      uses: subosito/flutter-action@v2.16.0
      with:
        flutter-version: '3.22.2' # Aik solid aur secure stable version jo dependency mismatch nahi karega
        channel: 'stable'
        cache: true

    - name: Install Dependencies
      run: |
        flutter pub upgrade --major-versions
        flutter pub get

    - name: Build Flutter Web
      run: flutter build web --release

    - name: Inject Isar Wasm
      run: |
        mkdir -p build/web
        curl -L "https://github.com/isar/isar/releases/download/3.1.0%2B1/isar.wasm" -o build/web/isar.wasm || echo "Isar WASM download failed, continuing..."

    - name: Deploy to Netlify
      env:
        NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
        NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
      run: |
        npm install -g netlify-cli
        netlify deploy --dir=build/web --prod --auth=$NETLIFY_AUTH_TOKEN --site=$NETLIFY_SITE_ID